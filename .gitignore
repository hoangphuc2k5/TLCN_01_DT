# Dependencies
node_modules/

# Env (secrets) — dùng .env.example làm mẫu
.env
.env.development
.env.production
.env.local
!.env.example
!.env.*.example

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Build
dist/
build/
.codegraph/

# Local browser test artifacts
ReactJS/test-results/
ReactJS/playwright-report/
ExpressJS/storage/
# Encrypted snapshots and interrupted publication artifacts are never source files.
*.edubak
*.edubak.partial-*
*.edubak.checking-*
